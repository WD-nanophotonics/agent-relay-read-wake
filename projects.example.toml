[account]
address = ""

[protocol]
prefix = "[AUTOMATION]"
legacy_prefix = ""

[[projects]]
code = ""
aliases = []
root = ""
# Omit inbox for <root>/inbox/<project-id>, or set a relative/absolute path.
branch = ""
remote = "origin"
push = false
legacy_prefixes = []
# Optional default ChatGPT conversation URL. A registered project URL takes precedence.
chat_url = ""

# Replace the empty values with the caller's account and project settings.
# Add one [[projects]] block per routed project. Codes must be unique.
